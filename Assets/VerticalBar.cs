using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VerticalBar : MonoBehaviour
{
    // Update is called once per frame
    void FixedUpdate()
    {
        //transform.position = new Vector3(HackManager.Instance.HexGrid[HackManager.Instance.currentAnchor][0].transform.position.x, -15, 0);
    }
}
